<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.juanipis.FanCtl.Helper</string>

    <key>BundleProgram</key>
    <string>Contents/MacOS/com.juanipis.FanCtl.Helper</string>

    <key>MachServices</key>
    <dict>
        <key>com.juanipis.FanCtl.Helper</key>
        <true/>
    </dict>

    <!-- Identifier-only requirement. With ad-hoc signing there is no
         certificate anchor, so we accept any binary signed with the same
         designated identifier. Tighten to "anchor apple generic and
         identifier ..." once a Developer ID is available. -->
    <key>SMAuthorizedClients</key>
    <array>
        <string>identifier "com.juanipis.FanCtl"</string>
    </array>

    <key>RunAtLoad</key>
    <false/>

    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
    </dict>
    <!-- Logging goes through os.Logger / unified log; inspect with:
         sudo log stream --predicate 'subsystem == "com.juanipis.FanCtl"' -->
</dict>
</plist>
